# Example .env for `frontend` (do NOT commit real secrets)
# Copy to `frontend/.env` and fill in real values for local development.

VITE_API_URL=https://your-api-url.example.com
VITE_SOME_PUBLIC_KEY=your_public_key
